"use client";
import { useState } from "react";
export default function DocumentAccessPage() { const [token, setToken] = useState(""); const [message, setMessage] = useState(""); return <main className="mx-auto max-w-md p-6"><h1 className="text-2xl font-black">Acceso a documentos</h1><form className="mt-4 grid gap-3" onSubmit={async (event) => { event.preventDefault(); const response = await fetch("/api/documents/session", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ token }) }); setToken(""); setMessage(response.ok ? "Acceso habilitado." : "No se pudo habilitar el acceso."); }}><input aria-label="Token de acceso" className="rounded-md border p-3" onChange={(event) => setToken(event.target.value)} type="password" value={token} /><button className="rounded-md bg-sea p-3 font-black text-white" type="submit">Entrar</button></form><p className="mt-3 text-sm">{message}</p><button className="mt-4 text-sm underline" onClick={() => void fetch("/api/documents/session", { method: "DELETE" })} type="button">Cerrar sesion</button></main>; }
